import { ref } from "vue";

export const useSitePaths = ref([
    { name: 'Inicio', href: '/' },
    { name: 'Servicios', href: '/servicios' },
    // { name: 'Contacto', href: '/contacto' }
]);